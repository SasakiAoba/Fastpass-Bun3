# 2026-09-14 リリース前検証

## 公開状態

ローカル実装と検証は完了。公開は未実施。

Cloudflare Pages `b2fp` の Production と Preview は、利用者の希望により同じ D1 `bun2fastpass` を使用する。新規D1は作成せず、schema 5で環境別のモード状態・業務領域を設ける。認証設定・端末は共有し、本番券とテスト券は区別する。

公開切替前の確認値は DEVELOPMENT、DEV 券226枚、LIVE 券0枚、スキーマ3。両環境のテストデータを削除して空で開始する指示を受領済み。

## 実装

- Production の自動開発者モード開始を無効、Preview ビルドでは有効にする。
- 開催日は既存の3日構成を維持し、2026年9月18日・19日・20日。管理画面は実際の運用日の値を表示する。
- iPad 横画面でのヘッダー折り返しと操作領域のはみ出しを修正。
- 古い同期応答や認証エラーが、確定済み操作後の新しい画面状態を上書きしないようにする。
- 同時更新の競合後は状態を読み直し、変更を再送しない。
- 通信断・5xx・不正な成功応答では操作IDごとに結果照合を継続し、結果不明中の追加確定を停止。再読み込み、複数タブにも対応。
- DEV削除後も操作の完了・削除済みを照合できる最小限の記録を保持。券・販売・返金の内容は保持しない。
- 変更がない定期同期は304応答にし、全状態の読み取りを省く。
- DEV開始・LIVEリセットで日付を保持し、販売がある開催中・将来日付のLIVEリセットを拒否。

## 検証結果

| 検証 | 結果 |
|---|---|
| 自動テスト | 11ファイル・54件成功 |
| TypeScript（フロント・Functions） | 成功 |
| Production / Preview 個別ビルド | 成功 |
| Pages Functions ビルド | 成功 |
| ローカルD1結合テスト | 成功 |
| 差分の空白検査 | 成功 |
| Codex内ブラウザ 1024×768 横画面 | ホーム・販売・入場受付・記録・会計・管理で横はみ出しなし |
| Codex内ブラウザ 1180×820 横画面 | 同6画面で横はみ出しなし |
| 販売・入場受付の視覚確認 | 1024×768でテンキー・確定ボタンが画面内に収まる |
| ローカル画面操作 | 販売、受渡し、入場、使用取消、払い戻し、会計を確認 |
| 別セッションの同時受付 | 同じ券の入場は1件のみ成功、他方は競合として拒否 |
| 別セッションのD1試験 | 同時販売・入場、確定状態共有、片方のログアウトによる他方への影響なし |
| 初期モード | 同じローカルD1でProductionとPreviewのモード・券・操作履歴・ETagが独立することを確認 |

D1結合テストでは、誤パスワード直後の再ログイン、CSRF、直接発番、入場、使用取消、払い戻し、販売直後の使用、冪等性、条件付き同期、DEV削除後の操作照合、LIVEリセット・日付ガード、ログアウトも検証した。

実機iPad・Safariでの検証、および公開した両環境での分離・操作検証は未実施。ブラウザ検証はCodex内ブラウザの表示サイズ設定を使用した。

## 公開手順

1. ProductionのTime Travelブックマークを取得し、切替中の旧版からのモード変更を止める。
2. 指示済みの共有DEVテストデータを削除し、LIVE券0枚・会計0円を照合する。
3. 新しいFunctionsより先に0004・0005を適用する。Productionの既存領域は保持する。
4. 同じDB Bindingのまま、Pages変数をProduction `FASTPASS_ENVIRONMENT=production`、Preview `FASTPASS_ENVIRONMENT=preview`にする。
5. 環境ごとに成果物をビルド・公開し、両方のhealth、schema 5、環境名、初期モードを確認する。
6. 環境分けを認識しない旧デプロイを撤去する。旧PreviewのFunctionsへロールバックしない。
7. Previewのテスト操作がProductionの券・会計・モード・同期判定に影響しないことを照合する。
8. Previewのテスト券を削除し、Production LIVE・Preview DEVELOPMENTで券0枚、会計0円、開催日、営業状態、外部キー整合性を確認する。
9. 最終のProductionとPreview URLを提出する。

無料プランの読み取り量・負荷は同じD1を使う両環境で共有する。304化の効果は主に無更新時にあり、更新が続く間の全状態同期は残る。営業中にPreviewで負荷試験を行わない。未解決の操作IDがある端末を旧クライアントへ戻すと保留状態の停止機構を失うため、その状態での旧版への差し戻しは行わない。
